// Configuración unificada con form_recomendantes/crear_usuario_recomendante (Office365)
$mail_smtp_server    = 'smtp.office365.com';
$mail_smtp_user      = 'sce.posgrado@inecol.mx';
$mail_smtp_pass      = '5C3.P0sgr4d0';
$mail_from           = 'sce.posgrado@inecol.mx';
$mail_to             = $para;
$mail_subject        = $asunto;
$mail_message        = $msj;
$mail_copies         = $copias;
$mail_format         = 'H';
$mail_tp_copies      = 'CCC';
$mail_port           = '587';
$mail_tp_connection  = 'T';

sc_mail_send($mail_smtp_server,
             $mail_smtp_user,
             $mail_smtp_pass,
             $mail_from,
             $mail_to,
             $mail_subject,
             $mail_message,
             $mail_format,
             $mail_copies,
             $mail_tp_copies,
             $mail_port,
             $mail_tp_connection);

sc_commit_trans();

if({sc_mail_ok}){
    return true;
}
else{
    sc_error_message({sc_mail_err});
    return false;
}
